Login - Needs provisioner ip return admin-auth-token
add microservice to provisioner(service-name, vendor, home-page-url) - Needs provisioner ip
delete microservice (microservice-name)
List microservice (token)
create Tenant(clusterName, contactPoints, keyspace, replicationType , replicas, driverClass, host, port, user, password )
List tenants ()
Assign identity microservice for tenant() - Get admin password (ambiguitiy)
Authenticate as an administrator
Create scheduler permission(permission-identifier)
Create scheduler user(identifier, password, role)




ip adress of all microservice
token
admin user
client-id
admin-password
